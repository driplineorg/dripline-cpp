language: cpp

dist: xenial

services:
  - docker

addons:
  apt:
    packages:
      - docker-ce

#branches:
#  only:
#    - master
#    - /^v\d+\.\d+\.\d+(-S*)?$/

jobs:
  include:
    - stage: environment setup
      script:
        - docker build -t driplineorg/dripline-cpp:$TRAVIS_BRANCH .
        - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
        - docker push driplineorg/dripline-cpp:$TRAVIS_BRANCH
